@{
    ViewData["Title"] = "Negocios";
}
@model List<ProductManagement.Core.Application.ViewModels.Business.BusinessViewModel>;

<div class="products">
    <div class="d-flex justify-content-between flex-wrap">
        <h3 class="display-6">Negocios</h3>
        <a type="button" asp-route-Id="" asp-action="Save" class="btn btn-info text-white" style="height: 40px;">Agregar un negocio</a>
    </div>
   

    <div class="products-container d-flex flex-wrap gap-3">
        @foreach (var item in Model)
        {
            @await Html.PartialAsync("~/Views/Shared/Components/Business/_Business.cshtml", item)
        }
    </div>
</div>


<!--
    a type="button" asp-action="Save" class="btn btn-success text-white" style="height: 40px;">Editar</a>
   <a type="button" asp-action="Save" class="btn btn-danger text-white" style="height: 40px;">Eliminar</a> 

-->  
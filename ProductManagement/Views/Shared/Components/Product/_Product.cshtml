@model ProductManagement.Core.Application.ViewModels.Product.ProductViewModel;

<div class="card card-product" style="width: 18rem;border-radius:10px;">
        <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
            <p class="card-text">@Model.Description</p>
            <p class="card-text">$@Model.Price pesos</p>
            <b class="card-text">@Model.Amount Unidades</b>
            <b class="card-text">@Model.CategoryName</b>
        <div class="buttons">
            <a asp-route-id="@Model.Id" asp-route-businessId="@ViewData["businessId"]" asp-route-categoryId="@Model.CategoryId" type="button" asp-controller="Product" asp-action="Edit" class="btn btn-success text-white" style="height: 40px;">Editar</a>
            <a asp-route-id="@Model.Id" asp-controller="Product" asp-action="Delete" asp-route-businessId="@ViewData["businessId"]" asp-route-categoryId="@Model.CategoryId" class="btn btn-danger text-white" style="height: 40px;">Eliminar</a>
        </div>
        </div>
</div>

<style>
    .card-product {
        transition: all 300ms;
    }
    .card-product a {
        text-decoration: none;
        
    }
     .card-product:hover {
        transform: scale(0.98);
     }
</style> 